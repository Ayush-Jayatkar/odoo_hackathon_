# JWT Secret used for signing authentication tokens
# You can generate a secure one using: openssl rand -base64 32
JWT_SECRET="super-secret-jwt-key-replace-in-production"

# (Optional) Resend API Key for sending actual emails
# Leave blank to fallback to console.log simulated emails
RESEND_API_KEY=""
